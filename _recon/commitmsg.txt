feat(deliver): dylib 构建通过并独立复核 —— 交付探针

CI（run 35523085355）结果：
  Build BiliProbe.dylib  ✓
  Verify dylib           ✓
  Package for signing    ✓
  Upload dylib           ✓
  Upload plugin package  ✓
  Release dylib only     ✘ 仅此一步失败

产物实测（Verify 输出）：
  Mach-O 64-bit dynamically linked shared library arm64
  依赖 6 个，全部系统库：libobjc.A / Foundation / CoreFoundation /
    UIKit / AVFoundation / libSystem.B
  install name = @executable_path/Frameworks/BiliProbe.dylib

本地独立复核 _recon/verify_dylib.py（不依赖 macOS 工具，自己解析 Mach-O）：
  ✔ arm64 瘦切片、MH_DYLIB
  ✔ install name 正确
  ✔ 未加密（cryptid 全 0）
  ✔ 6 个依赖全为系统库/自身路径
  ✔ 平台 iOS、部署目标 14.0.0（与 App 主二进制 minos 一致）
  ✔ 已带 ad-hoc 代码签名
  ✔ 含 BiliProbe / biliprobe 标记
  sha256 = 16A64214F6130A82F0387717930079CD31693C28B10BA98F3B61E7B9B8ED747F

修复：
  * workflow 加 permissions: contents: write —— 否则建 release 报
    "Resource not accessible by integration"
  * verify_dylib.py 第一版把「存在 LC_ENCRYPTION_INFO_64」误判为已加密；
    链接器默认就会发这条命令，正确判据是 cryptid 是否为 0。已改正。

交付：deliver/BiliProbe.dylib（114,688 字节）+ 使用说明。
