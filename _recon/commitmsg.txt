chore: 停止跟踪 _thirdparty/ 字符串转储（第三方内容）

上次提交把从第三方 dylib 提取的字符串转储一并入库了：
  _thirdparty/BiliBiliTweak.dylib.strings.txt   7254 行
  _thirdparty/BiliNoAds.dylib.strings.txt        927 行
  _thirdparty/B站空降助手…dylib.strings.txt      1647 行
  _thirdparty/recon_*.json

这些是从**第三方二进制**里抽出的字符串，与本仓库自己的代码无关，
体量大（近万行）、含第三方内容，且本地随时可由
_recon/dissect_tweaks.py / recon_thirdparty.py 重新生成。

处理：
  * git rm --cached 移除跟踪（本地文件保留，不影响后续分析）
  * .gitignore 加入 _thirdparty/
  * preflight 行尾检查排除 _thirdparty/（与 deliver/ _recon/ 同理：
    这些是本地分析产物，不参与 CI）

注：preflight 的行尾闸门这次正常工作 —— 它在提交前就报出了这 3 个文件，
是我在提交时没有先看闸门输出。已改正。
