# 脱壳 IPA 与解包产物：体积大且涉及第三方版权，绝不入库
*.ipa
*.tipa
*.app/
Payload/
work/
work2/

# 会话导出与侦察中间产物
# （keyword_groups/recon 含从官方二进制提取的字符串，属第三方内容，不入远端）
_session_extract/
dsh-session-session-*.zip
_recon/strings_all.txt
_recon/objc_methods.tsv
_recon/keyword_groups.json
_recon/recon.json
# 注意：_recon/sidx_fixtures/ 是**自行构造**的测试夹具（非官方数据），
# 需要入库供 CI 使用，故不在此忽略。

# 构建产物
dist/
.theos/
inject/probe/.theos/
*.dylib
*.o

# 系统噪音
.DS_Store
Thumbs.db
__pycache__/
*.pyc

# CI 产物本地副本
_artifact/
deliver/

# 第三方 IPA 的分析产物（字符串转储等），含第三方内容，不入库
_thirdparty/

# 真机回传的日志包（内含第三方 App 的运行时类转储，不入库）
biliprobe*.zip
_logs/
_biliprobe/
_biliprobe2/
_biliprobe3/
_biliprobe4/
_biliprobe5/
_biliprobe6/
_biliprobe7/
_biliprobe8/
biliprobe/


# 真机回传的单文件日志
trace.log

# 真机回传的 BiliFast 日志 / 报告 / 压缩包（用户数据，不入库）
/BiliFast*.log
/BiliFast*.zip
/report*.txt

# 从 CI 下载 artifact 的临时解包目录
/_dl_*/
